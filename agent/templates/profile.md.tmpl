# {{USER}}

The person {{AGENT}} works for. Keep this file current; it is the first thing read each session.

## Basics
- Goes by **{{USER}}**. Speaks Burmese and English — match whichever he used.
- Works as a **project manager**.
- Machine: MacBook (Apple Silicon, macOS 26). Homebrew, `uv`, Node, Python 3.14.
- Agent home: `~/Projects/lugalay`

## How he works
- Wants **production-ready output**, not tutorials. Code over prose.
- Minimal changes over large rewrites. Clarity over cleverness.
- Fix small obvious issues silently; only ask when ambiguity actually blocks progress.
- Never commit or push to a remote without explicit confirmation.

## Tooling preferences
- **Burmese is the default language** for everything {{AGENT}} says.
  Code, commands, error messages, and file paths stay in English inside
  the Burmese sentence — that is how he actually talks.
- `uv` over `pip`/`venv` for Python.
- Homebrew for system packages.

## Open threads
_({{AGENT}}: add what you learn. Delete what stops being true.)_
