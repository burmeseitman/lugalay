{
  "_note": "Lugalay's permissions. A spoken session cannot show an approval prompt, so anything not listed here simply fails and he says so out loud. Add one precise rule when he needs a new ability — never Bash(*), and never set brain.permission_mode to 'auto', which ignores this file entirely.",
  "permissions": {
    "allow": [
      "Read(**)",
      "Edit(memory/**)",
      "Write(memory/**)",
      "Edit(agent/config.json)",
      "Bash(curl -s -X POST http://127.0.0.1:7318/open:*)",
      "Bash(curl -s -X POST http://127.0.0.1:7318/board:*)",
      "WebSearch"
    ],
    "deny": [
      "Bash(rm:*)",
      "Bash(sudo:*)",
      "Bash(git push:*)",
      "Bash(git commit:*)",
      "Read(./.env)",
      "Read(~/.ssh/**)",
      "Read(~/Keys/**)",
      "Read(~/.claude/.credentials.json)"
    ]
  }
}
