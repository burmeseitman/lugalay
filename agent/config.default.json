{
  "name": "Lugalay",
  "user": "",
  "greeting": "မင်္ဂလာပါ {user}။ ဒီနေ့ ဘာအစီအစဉ်ရှိလဲ။",
  "face": "aung",
  "face_port": 7317,
  "hands_port": 7318,
  "mic": {
    "mode": "open",
    "key": [
      "<cmd_r>",
      "<alt_r>"
    ],
    "max_seconds": 30,
    "samplerate": 16000,
    "device": null,
    "_device_note": "null = system default. Or an index/name substring from mictest.py.",
    "debug_keys": true,
    "_key_note": "pynput syntax; a list means any of them works. e.g. <cmd_r>, <alt_r>, <f13>",
    "open_sensitivity": 3.5,
    "open_silence_seconds": 0.6,
    "open_min_seconds": 0.3,
    "fast_response": true,
    "_mode_note": "open = hands-free (just talk). ptt = hold a key.",
    "_silence_note": "how long you must stop talking before it replies. Lower = snappier, but cuts you off mid-pause."
  },
  "stt": {
    "engine": "faster-whisper",
    "google": true,
    "mode": "bilingual",
    "beam_size": 5,
    "lid_model": "base",
    "models": {
      "my": "whisper-my-turbo",
      "en": "small.en"
    },
    "compute_type": "int8",
    "_note": "Whisper's language id never returns 'my' for real Burmese (it says zh/th/cy). It identifies English reliably, so the router asks only 'was that English?' and treats everything else as Burmese. Raise english_min_prob to bias further toward Burmese.",
    "english_min_prob": 0.5,
    "_models_note": "whisper-my-turbo (809M, ~3.5s) is the default. whisper-my-large (1.5B, ~14-21s) is also converted and gives longer, more coherent Burmese but is 4-6x slower — swap models.my to 'whisper-my-large' to try it."
  },
  "tts": {
    "engine": "gemini",
    "prefer_gemini": true,
    "gemini": {
      "model": "gemini-3.1-flash-lite",
      "max_tokens": 180,
      "timeout_seconds": 30,
      "_note": "Uses the securely saved TTS key. Flash-Lite streams the first sentence with minimal thinking; local Ollama remains the offline fallback."
    },
    "provider": "auto",
    "gemini_api_key": "",
    "api_key": "",
    "_api_key_note": "Add or remove the Gemini key from the in-app Settings panel. GEMINI_API_KEY and GOOGLE_API_KEY are also supported. api_key is retained only for older configs.",
    "gemini_model": "gemini-2.5-flash-preview-tts",
    "gemini_voice": "",
    "gemini_timeout_seconds": 45,
    "_gemini_note": "auto uses Gemini for Burmese when a key is available, then falls back to Edge. Set provider to edge to force Edge. An empty gemini_voice selects Achird for male faces or Aoede for female faces.",
    "voice": "bm_lewis",
    "burmese_voice": "my-MM-ThihaNeural",
    "speed": 1.0,
    "fallback_voice": "Daniel",
    "burmese_rate": "+0%",
    "burmese_pitch": "+0Hz"
  },
  "brain": {
    "cmd": "claude",
    "permission_mode": "ask",
    "continue_session": true,
    "timeout_seconds": 180,
    "fast": true,
    "effort": null,
    "_fast_note": "fast adds --strict-mcp-config and --disable-slash-commands; measured ~1.5s off every reply. Set false if Lugalay ever needs an MCP server or a skill mid-conversation.",
    "stream": true,
    "_stream_note": "Speak the first sentence while the rest is still being written. Measured on an M3 Pro: Burmese first sound at 6.8s instead of 12.9s. Burmese streams through one Burmese voice in sentence-sized units, so the accent never changes mid-answer — the thing that made this whole-block in the first place. Set false to go back to holding the reply until it is finished.",
    "engine": "auto",
    "ollama": {
      "url": "http://127.0.0.1:11434",
      "model": "qwen3:8b",
      "max_tokens": 220,
      "think": false,
      "_think_note": "qwen3 is a reasoning model; left on, it spends the whole token budget inside <think> and returns empty content.",
      "keep_alive": "30m",
      "_keep_alive_note": "How long Ollama holds the model in memory after a reply. Default 5m means a normal pause in conversation costs a 4-9s reload on the next question. '-1' never unloads, at the price of ~5GB of RAM sitting there.",
      "_model_note": "A preference, not a requirement. If this model is not installed, the smallest one that is gets used instead and the log says so — deleting a model used to leave the fallback calling something Ollama no longer had, which failed silently. Cloud-hosted entries (name ending :cloud) are skipped, since a fallback for being offline cannot itself need the internet."
    },
    "_engine_note": "gemini = fastest conversational path using the securely saved Gemini key, with local fallback. auto = Gemini when its key exists, otherwise Claude and local fallback. openai / groq / together / openrouter / deepseek use brain.remote. ollama / lmstudio use a local model server.",
    "remote": {
      "api_key": "",
      "url": "",
      "model": "",
      "_note": "A hosted OpenAI-compatible model. Set brain.engine to one of 'openai', 'groq', 'together', 'openrouter', 'deepseek' to use one of the known presets; url and model can be left empty then and the preset fills them in. For anything else — a self-hosted endpoint, Fireworks, xAI — set engine to 'openai' and give url and model here. The key can also come from the OPENAI_API_KEY environment variable. Local backends (ollama, lmstudio) do not use this section."
    }
  },
  "language": {
    "listen": "my",
    "reply": "my",
    "_note": "listen: 'my' (Burmese), 'en' (English), 'auto' (Bilingual). reply: 'my' (Burmese), 'en' (English), 'auto' (Match spoken)",
    "my_prompt_file": "",
    "_prompt_note": "How he speaks each language lives in agent/voice/prompts/*.md and is re-read every turn, so edits land on the next sentence. my_prompt_file points somewhere else entirely if you want to keep your own version outside the repo; empty means use the shipped one."
  },
  "window": {
    "width": 460,
    "height": 640,
    "x": null,
    "y": null,
    "frameless": true,
    "always_on_top": false,
    "_note": "The window is transparent and frameless so overlay mode (key B) can strip everything but the avatar. In normal mode the page paints its own dark card, so it still looks like a window. Drag anywhere to move it; Cmd-Q quits.",
    "transparent": true,
    "background_color": "#04070A",
    "overlay": false,
    "_overlay_note": "true starts bare: no card, no bar, no caption, just the avatar. Key B toggles it at runtime."
  },
  "faces": [
    {
      "id": "aung",
      "name": "Aung",
      "gender": "male",
      "age": 24,
      "skin": "#d9a279",
      "shade": "#b8825c",
      "hair": "#33261f",
      "style": "short",
      "shirt": "#2d606c",
      "beard": false,
      "wrinkles": false,
      "voice": {
        "gemini": "Achird",
        "my": "my-MM-ThihaNeural",
        "my_rate": "+2%",
        "my_pitch": "+0Hz",
        "en": "am_adam"
      }
    },
    {
      "id": "hnin",
      "name": "Hnin",
      "gender": "female",
      "age": 22,
      "skin": "#e0ae87",
      "shade": "#bd8a64",
      "hair": "#2a1f1b",
      "style": "long",
      "shirt": "#7a4f6e",
      "beard": false,
      "wrinkles": false,
      "voice": {
        "gemini": "Leda",
        "my": "my-MM-NilarNeural",
        "my_rate": "+0%",
        "my_pitch": "+1Hz",
        "en": "af_heart"
      }
    },
    {
      "id": "zaw",
      "name": "Zaw",
      "gender": "male",
      "age": 42,
      "skin": "#c9905f",
      "shade": "#a3703f",
      "hair": "#241a15",
      "style": "short",
      "shirt": "#33526b",
      "beard": true,
      "wrinkles": false,
      "voice": {
        "gemini": "Charon",
        "my": "my-MM-ThihaNeural",
        "my_rate": "-2%",
        "my_pitch": "-3Hz",
        "en": "bm_fable"
      }
    },
    {
      "id": "mya",
      "name": "Mya",
      "gender": "female",
      "age": 40,
      "skin": "#d7a077",
      "shade": "#b07f56",
      "hair": "#33241d",
      "style": "bun",
      "shirt": "#6b4a3a",
      "beard": false,
      "wrinkles": false,
      "voice": {
        "gemini": "Gacrux",
        "my": "my-MM-NilarNeural",
        "my_rate": "-3%",
        "my_pitch": "-2Hz",
        "en": "bf_isabella"
      }
    },
    {
      "id": "uba",
      "name": "U Ba",
      "gender": "male",
      "age": 68,
      "skin": "#c59a74",
      "shade": "#a2795a",
      "hair": "#b9b3ab",
      "style": "short",
      "shirt": "#473b32",
      "beard": false,
      "wrinkles": true,
      "voice": {
        "gemini": "Algenib",
        "my": "my-MM-ThihaNeural",
        "my_rate": "-6%",
        "my_pitch": "-7Hz",
        "en": "bm_george"
      }
    }
  ],
  "_faces_note": "press 1-5 to switch. Gemini uses a distinct voice per face; the Edge fallback uses Thiha/Nilar with age expressed through rate and pitch.",
  "greeting_en": "Hello {user}, what are we working on today?",
  "setup_done": false,
  "_setup_note": "setup_done false makes the app open its first-run screen instead of the face. The name lives here and only here; CLAUDE.md and memory/profile.md are rendered from agent/templates/ during setup.",
  "eyes": {
    "enabled": true,
    "device": 0,
    "_note": "Lugalay's eyes: when you ask him to look at something, one webcam frame is captured and handed to the brain, which reads the image itself. Nothing is streamed and nothing is kept but the last frame. device 0 is the built-in camera; raise it for an external one."
  }
}
